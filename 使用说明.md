# FishReader 悬浮阅读器

双击 `启动阅读器.cmd` 即可运行。第一次启动后，在透明窗口内右键，选择“打开小说”。

支持：TXT、EPUB、HTML、Markdown、FB2、RTF、DOCX（仅文本型内容）。PDF、MOBI/AZW3 与受 DRM 保护的电子书暂未纳入此便携版本。

操作：鼠标左键拖动窗口；双击窗口保存进度并关闭；滚轮向下/向上翻页；右键打开功能菜单。显示区域可在右键菜单内调整，右下角不会显示缩放三角。

右键菜单内的“行间距”和“段间距”均提供紧凑、标准、宽松和微调；“字体颜色”提供多种预设及系统颜色选择器。所有选项都会自动保存。


全局快捷键：

- `Ctrl + Alt + H`：关闭阅读器（下次启动自动续读）
- `Ctrl + Alt + T`：开启或关闭鼠标穿透
- `Esc`：关闭阅读器（窗口获得焦点时）

所有阅读记录和设置会保存在同目录的 `FishReaderData`，复制整个文件夹即可迁移到另一台 Windows 电脑。
